{
  "name": "video-project-workspace",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present && vitest run tests/integration"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.8.0"
  }
}